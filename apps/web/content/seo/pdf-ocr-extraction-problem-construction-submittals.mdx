---
title: "Why does AI still struggle to read your submittal PDFs?"
description: "AI submittal tools read typed spec sections and cut sheets well, but choke on scanned faxes, hand-marked shop drawings, and dense equipment tables. Here's where the extraction layer actually breaks, and what shipped in 2026 to fix it."
target_keyword: "PDF OCR extraction problem construction submittals"
audience: "gc_ops"
updated_at: "2026-07-16"
sources:
  - "https://mistral.ai/news/ocr-4/"
  - "https://www.marktechpost.com/2026/06/23/mistral-ocr-4/"
  - "https://www.techtimes.com/articles/318978/20260624/mistral-ocr-4-ships-structure-aware-document-ai-runs-your-own-infrastructure.htm"
  - "https://www.globenewswire.com/news-release/2026/06/17/3313698/0/en/trunk-tools-launches-cortex-to-tackle-construction-s-hardest-ai-problem-drawings.html"
  - "https://www.enr.com/articles/61435-gilbane-rolls-out-trunk-tools-ai-agents-across-its-jobsites"
  - "https://buildsync.ai/resources/the-true-cost-of-a-rejected-submittal-805-each"
faqs:
  - q: "Why can't AI just OCR my submittal PDFs and compare them to the spec in one step?"
    a: "Because reading a page and understanding its structure are two different problems. A basic OCR pass turns a PDF into a wall of text, which is enough to search but not enough to compare a product's voltage rating against a spec paragraph. Structure-aware extraction — the kind Mistral's OCR 4 or Trunk Tools' Cortex platform do — has to first identify tables, headers, and field labels correctly before any spec comparison can run on top of it. Skip that step and the comparison layer is working off garbage."
  - q: "Does AI read construction drawings as well as it reads spec text?"
    a: "No, and that gap is the core of the extraction problem. Specs, product data sheets, and O&M submittals are text-heavy and extract cleanly. Drawings are visual and spatial — dimension strings, revision clouds, symbol layers — and general-purpose vision models weren't built for that convention. Trunk Tools says it spent four years training bespoke models on 2D construction drawings before its Cortex platform could reliably read a drawing bulletin, which tells you how much harder that half of the problem is than the text half."
  - q: "What does it cost to fix the extraction layer on a submittal log?"
    a: "The ingestion piece got a lot cheaper in 2026. Mistral's OCR 4, released in June, prices structured extraction at $2 to $5 per 1,000 pages and can run self-hosted. A full job's submittal log — a few hundred packages at five to ten pages each — runs somewhere between $10 and $30 to process at the batch rate. That's the extraction cost only; the spec-comparison logic on top of it still has to exist somewhere, whether that's a GC platform agent or a tool you build."
  - q: "Does a GC need to self-host OCR instead of using a cloud API?"
    a: "Only if the documents carry data you can't send to a third-party server — subcontract pricing, change order unit costs, or a federal project with CUI handling requirements in the contract. Most cloud OCR services transmit documents to provider infrastructure by default. Mistral OCR 4 ships as a single Docker container specifically so a GC or sub with that constraint can run extraction on its own network instead."
  - q: "If the extraction layer gets fixed, does AI submittal review stop needing a human reviewer?"
    a: "No. Better extraction removes a bottleneck upstream of the actual decision — it doesn't make the decision. A clean read of a cut sheet still hands off to a person for anything that requires judgment: a coordination clash that only shows up when trades are overlaid, or a substitution that's technically compliant but changes lead time or maintenance access. The PE or engineer of record still signs off on every item."
---

AI submittal review has been marketed as a spec-comparison problem — feed it a cut sheet, feed it a spec section, get a compliance flag back. In practice it's an extraction problem first. Most submittal packages aren't clean, uniform PDFs: they're scanned faxes, phone-photographed shop drawings, and dense equipment tables buried inside a 40-page manufacturer catalog excerpt. Before any AI can compare a submittal against a spec, it has to correctly pull structured data — the right table, the right field, the right dimension — out of a document that wasn't built to be machine-read. That step is where most of the failures actually happen, and it's the layer the industry only started seriously fixing in mid-2026.

## What actually goes wrong when AI tries to read a submittal PDF?

Two different things fail, and they fail for different reasons.

The first is raw text extraction on a bad scan: fax artifacts, skewed pages, a stamp overlapping the product table, a fifth-generation photocopy of a cut sheet. Standard OCR degrades here the same way it always has — wrong characters, missing rows, a table that reads as a paragraph.

The second, more common failure isn't about legibility at all. It's about structure. A cut sheet's product table might scan perfectly and still get misread if the extraction tool doesn't know that column three is voltage and column five is a footnote, not a value. That's the difference between OCR — turning pixels into text — and structured extraction, which returns labeled fields, table cells, and document sections an AI can actually reason over. A submittal comparison built on unstructured text output will misfire even on a document that scanned cleanly.

## Why do construction drawings extract so much worse than spec text?

Because they're a different kind of document, not just a harder version of the same one. Specs, product data sheets, and O&M submittals are text-heavy — paragraphs, tables, numbered requirements — and current extraction models handle that format well. Drawings are visual and spatial: dimension strings, revision clouds, symbol layers, notes that only make sense relative to where they sit on the sheet. Most AI tools embedded in construction platforms, including the agent suites Procore and others shipped through 2026, process text-based documents reasonably well and treat drawings as a separate, harder problem.

Trunk Tools built its Cortex platform specifically to close that gap, and the company says it took four years of training bespoke models on 2D construction drawings to get there — a specific enough claim about the difficulty that it's worth taking at face value. At launch, Trunk Tools reported Cortex reviewing a 20-sheet change bulletin, including changes the architect didn't cloud, in under five minutes ([GlobeNewswire](https://www.globenewswire.com/news-release/2026/06/17/3313698/0/en/trunk-tools-launches-cortex-to-tackle-construction-s-hardest-ai-problem-drawings.html); [our full breakdown of Cortex's launch](/posts/2026-06-21-trunk-tools-cortex-construction-drawings-ai)). That's a text-and-drawing-reading problem, not a pure OCR problem — which is exactly why it took years longer to ship than text-only submittal review did.

## What's actually shipping to fix the extraction layer?

The clearest 2026 development is on the text side. Mistral released OCR 4 in June, a document intelligence model built for structured extraction rather than plain OCR: it returns text blocks with page coordinates, labels each block by type (table, header, footnote, signature), and attaches confidence scores at the word and block level. It processes PDF, DOC, PPT, and OpenDocument formats, and — notably for a sub whose submittal packages carry pricing or manufacturer terms — it deploys as a self-hosted Docker container instead of requiring documents to leave your network ([Mistral](https://mistral.ai/news/ocr-4/)). Independent benchmark testing (OlmOCRBench) put it ahead of every competing system tested in the majority of head-to-head comparisons, though that number comes from Mistral's own release materials, not a third-party construction-document evaluation ([MarkTechPost](https://www.marktechpost.com/2026/06/23/mistral-ocr-4/); [Tech Times](https://www.techtimes.com/articles/318978/20260624/mistral-ocr-4-ships-structure-aware-document-ai-runs-your-own-infrastructure.htm); [our workflow breakdown of OCR 4 for submittals](/posts/2026-06-28-mistral-ocr-4-submittal-extraction-construction)).

The realistic pipeline this enables: submittal PDF and spec section both go through structured extraction first, then a separate comparison model — a GC platform's agent, or a purpose-built tool — reasons over the structured output to flag mismatches. Extraction and comparison are still two distinct jobs. Fixing the first doesn't automatically fix the second.

## Extraction approaches compared

| Approach | What it handles well | Where it still breaks | Rough cost |
|---|---|---|---|
| **Mistral OCR 4 (self-hosted or API)** | Text-heavy documents: spec sections, product data sheets, O&M submittals; structured table and field output | Dense graphical drawings with heavy spatial reliance — architectural sheets, structural details, MEP symbol layers | $2–$5 per 1,000 pages; self-hostable at no per-page fee beyond compute |
| **Trunk Tools Cortex / TrunkSubmittal** | Both text and 2D drawings, purpose-built for construction document conventions; pulls related spec sections and RFI context automatically | Judgment calls on ambiguous spec language or substitution requests | Subscription, layered on top of Procore or Autodesk Construction Cloud |
| **Native GC platform OCR (baseline agents)** | Standard-format PDFs already organized inside the platform | Older scans, non-standard formats, anything outside the platform's document pipeline | Usually bundled into existing platform contract |
| **Manual review (status quo)** | Everything — a person can read anything eventually | Volume; a 200–400 item submittal log at two hours per review doesn't scale | Coordinator/PE labor hours |

## Is fixing extraction actually worth it, or is this a solved problem already?

For a GC or MEP sub whose submittal log clears 150+ items on a job, yes — extraction quality is the ceiling on how much of the review process AI can touch at all. Gilbane's rollout of Trunk Tools' submittal agent flagged as many as 72% of first-pass submittals as non-compliant, with only 3% fully compliant on the first read, and reported close to a 50% cut in re-submission cycle time once that first pass ran reliably ([ENR](https://www.enr.com/articles/61435-gilbane-rolls-out-trunk-tools-ai-agents-across-its-jobsites)). One vendor's own accounting puts the average cost of a rejected submittal at roughly $805 in administrative time plus 2 to 4 weeks of added cycle time ([BuildSync](https://buildsync.ai/resources/the-true-cost-of-a-rejected-submittal-805-each)) — a vendor number, but directionally consistent with why a broken extraction layer is expensive at volume, not just annoying.

If your log runs low volume and a coordinator with a clean template already keeps pace, the extraction problem isn't costing you enough yet to chase. The test that actually tells you something: run one Division 22 or 23 submittal package that includes at least one scanned or hand-marked page through whichever extraction tool your GC or vendor offers, and check whether the output correctly separates the product table from the footnotes before you trust it with a spec comparison. If it can't do that cleanly, no downstream AI review is going to be reliable either.

*For the full picture on where AI actually sits in the RFI, submittal, and change-order stack, see our [guide to AI for RFIs, submittals, and change orders](/seo/ai-construction-rfi-submittal-change-order-guide) — or, if you're a mechanical or MEP sub asking whether the review layer itself is worth piloting, [does AI submittal review actually work for mechanical and MEP contractors?](/seo/ai-submittal-review-mechanical-mep-contractors) Construction AI Brief covers what's actually shipping for GCs, trade subs, and estimators — three times a week. [Subscribe at constructionaibrief.com](https://constructionaibrief.com/?utm_source=cab&utm_medium=web&utm_campaign=submittal_qa_cta).*
